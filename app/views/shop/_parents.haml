- if group.extra_picture.url
  - content_for(:extra_pic) do
    .extrapic
      = image_tag group.extra_picture.url(:original)
.col-md-3.col-sm-3
  .row
    .group-wrapper.side-tex
      %h5
        %a{href: "/"}
          =image_tag  "kauppa/aurinko.png"
          = t(:to_hompage)

    %br
    %b
      FARFALLA - Huippusarja maailmalta, nyt myös Suomessa!
      %br
      Sveitsiläistä laadukasta luomukosmetiikkaa: 100% luonnollisista ainesosista, joista suuri osa jopa yli 95% luomua (NaTrue***)
    - parents(group).each do | group|
      .group-wrapper.side
        = link_to shop_group_path(:link => group.link) do
          = image_tag group.main_picture.url(:product)
          %h5= group.name

      .intro-text
        %p.description
          = markdown group.summary

    .intro2.group-wrapper.side
      = link_to shop_group_path(:link => group.link) do
        = image_tag group.main_picture.url(:product)
        %h5= group.name
    .intro-text
      %p.description
        = markdown group.summary
        = markdown group.description

  .row
    .shipping-info.frame
      %a{href: "/page/toimitusehdot#toimituskulut"}
        %h5.highlight
          Edulliset toimituskulut,
          %br
          alkaen vain 3 €!
